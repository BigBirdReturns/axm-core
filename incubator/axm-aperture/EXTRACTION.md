# Dedicated-repository extraction transaction

Create `BigBirdReturns/axm-aperture` under Apache-2.0, place the candidate source at repository root, and verify all 188 paths in `MANIFEST.sha256`. Run `python scripts/qualify.py --write` on Linux and Windows with Python 3.11 and 3.13, Node 22, .NET 8, ffmpeg, and ffprobe. The semantic qualification identity must equal `qualification1_8ae7452d6f0d903fb27e81319fa83b59e4d0b0004f03124b191a3a1a607843a8` when the .NET lane has the same declared standing, and any expanded evidence must create a new, explicitly stronger receipt rather than rewriting this one.

After the dedicated source is hosted, open AP-308 from current Arc main to add the backwards-compatible timed-media verifier. Open AP-309 from current Core main to register rebuildable Spectra tables. Complete AP-311 through the pinned Genesis one-pass compiler and an out-of-band trusted key. Then rerun the exact two-edition oracle and classify G0, G1, and G2 from the complete cross-repository evidence set.

Refuse extraction if the archive digest differs, the source manifest does not reconcile, a consumer depends on an open donor branch, the canonical v1 story bytes drift, a DuckDB cache becomes normative, an embedded publisher key is treated as trusted, or any local receipt is described as hosted or physical acceptance.
