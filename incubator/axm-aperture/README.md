# AXM Aperture incubation custody

This directory is a temporary, never-merge custody surface for the AXM Aperture 0.1.0 implementation candidate. `BigBirdReturns/axm-core` does not acquire Aperture product authority. The intended repository is `BigBirdReturns/axm-aperture`, which does not yet exist and cannot be created through the currently connected GitHub surface.

The candidate implements the G0-G2 contract and exact-oracle plane: edition-aware playback anchors, piecewise provider-to-canonical TimeMaps, separate Exposure and Knowledge Ledgers, deterministic answer plans, same-work selection, observation-verified actuation, restart recovery, replay, backup, deletion, a scripted MPV adapter, a loopback API, and MCP. The complete source is carried by the content-addressed package identified in `CUSTODY.json`; the package is also available from the originating ChatGPT execution record.

This directory may be reviewed and used to create the dedicated repository. It must not be merged into Core main as permanent architecture. After extraction, only the narrow AP-309 rebuildable Spectra extension-table loader may return to Core through its own independently qualified pull request.

The control question is whether the exact source can leave temporary Core custody and return only a versioned query interface while Genesis, Arc, Core, World, players, and Aperture retain their declared authority boundaries.
